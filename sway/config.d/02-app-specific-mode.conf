# swaymsg -t get_tree

for_window [app_id="pavucontrol"] {
  floating enable
  sticky enable
  border normal
}

for_window [class="fcitx5-config-qt"] {
  floating enable
  sticky enable
  border normal
}
